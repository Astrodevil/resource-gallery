---
import { SEO } from "astro-seo";
import Footer from "@components/footer.astro";
import Navbar from "@components/navbar/navbar.astro";
import "@fontsource/inter/variable.css";


export interface Props {
  title: string;
}

// const { src } = await getImage({
//   src: "../../assets/hero.png",
//   width: 800,
//   height: 742,
//   format: "avif",
// });
// console.log(src);

const canonicalURL = new URL(Astro.url.pathname, Astro.site).toString();

const resolvedImageWithDomain = new URL(
  "/screely.png",
  Astro.site
).toString();

const { title } = Astro.props;

const makeTitle = title
  ? title + " | " + "Resource Gallery"
  : "Resource Gallery - Resources you need to start in tech";
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <!-- <link rel="icon" type="image/svg+xml" href="/favicon.svg" /> -->
    <meta name="generator" content={Astro.generator} />

    <!-- <link rel="preload" as="image" href={src} alt="Hero" /> -->
    <SEO
      title={makeTitle}
      description="Wants to start open-source? Are you a beginner? Don't know how, where, why to start? You are in right place.😊 Open-Source-Gallery provides you with all the resources needed to become a good open-source contributor, project maintainer & developer."
      canonical={canonicalURL}
      twitter={{
        creator: "@astrodevil_",
        site: "@resourcegallery",
        card: "summary_large_image",
      }}
      screely={{
        basic: {
          url: canonicalURL,
          type: "website",
          title: `Wants to start open-source? Are you a beginner? Don't know how, where, why to start? You are in right place.😊 Open-Source-Gallery provides you with all the resources needed to become a good open-source contributor, project maintainer & developer.`,
          image: resolvedImageWithDomain,
        },
        image: {
          alt: "Resource Gallery Homepage Screenshot",
        },
      }}
    />
  </head>
  <body>
    <Navbar />
    <slot />
    <Footer />
    <style is:global>
      
      /* Improve Page speed */
      /* https://css-tricks.com/almanac/properties/c/content-visibility/ */
      img {
        content-visibility: auto;
      }
    </style>
  </body>
</html>
